import { CAFormsModule, CAValidators, CAFormControl, CAFormGroup, CAFormArray, CAFormControlAbstract, CAFormComponent } from '../ca-forms';

describe('CAFormsModule, CAValidators, CAFormControl, CAFormGroup, CAFormArray, CAFormControlAbstract, CAFormComponent', () => {

  it('CAFormsModule, CAFormAbstract, CACrudFormAbstract was exported', () => {
    expect(true).toBeTruthy();
  });

});
